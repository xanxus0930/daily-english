# Daily English → 放到手機主畫面的完整教學

## 方法：GitHub Pages（免費，5分鐘完成）

GitHub Pages 會給你一個真實網址，Safari 就能直接開啟並加入主畫面。

---

## 步驟一：建立 GitHub 帳號

1. 電腦瀏覽器開啟 → https://github.com
2. 點右上角「Sign up」
3. 填寫 Email、密碼、帳號名稱（帳號名稱會變成網址的一部分）
4. 完成驗證

---

## 步驟二：建立新的 Repository

1. 登入後點右上角「+」→「New repository」
2. Repository name 填：`daily-english`
3. 選「Public」（必須是 Public 才能免費用 Pages）
4. 勾選「Add a README file」
5. 點「Create repository」

---

## 步驟三：上傳檔案（共 6 個）

1. 把電腦上的 `index-v3-20260822.html` **先改名為 `index.html`**
2. 在 Repository 頁面點「Add file」→「Upload files」
3. 把這 6 個檔案一起拖進去（檔名不能改，全部放在根目錄）：

   | 檔案 | 用途 |
   |---|---|
   | `index.html` | App 本體 |
   | `sw.js` | 離線快取（Service Worker） |
   | `manifest.webmanifest` | App 名稱、圖示、全螢幕設定 |
   | `icon-180.png` | iPhone 主畫面圖示 |
   | `icon-192.png` | Android 圖示 |
   | `icon-512.png` | 安裝畫面大圖示 |

4. 點「Commit changes」

⚠️ 少傳任一個都會讓離線功能失效（sw.js 安裝時會一併快取這些檔，其中一個 404 就整個失敗）。

---

## 步驟四：開啟 GitHub Pages

1. 點 Repository 頁面上方的「Settings」
2. 左側選單找到「Pages」
3. Source 選「Deploy from a branch」
4. Branch 選「main」，資料夾選「/ (root)」
5. 點「Save」
6. 等 1-2 分鐘後，頁面頂部會出現綠色框框顯示你的網址：
   `https://你的帳號名稱.github.io/daily-english`

---

## 步驟五：iPhone 加入主畫面

1. **用 iPhone 的 Safari** 開啟你的網址
2. 點底部中間的「分享」按鈕（方框加箭頭的圖示）
3. 往下滑找「加入主畫面」
4. 名稱改成「Daily English」
5. 點右上角「新增」
6. 桌面就會出現 App 圖示！

---

## 步驟五（Android）：加入主畫面

1. 用 **Chrome** 開啟網址
2. 點右上角三個點「⋮」
3. 選「新增到主畫面」或「安裝應用程式」
4. 確認即可

---

## 完成後的效果

✅ 點圖示開啟是全螢幕，沒有瀏覽器工具列  
✅ 離線可用：第一次聯網開啟後，sw.js 會把 App 存進本機，之後飛航模式也能背單字  
✅ 學習進度儲存在手機裡不會消失  
✅ 可以分享網址給朋友

⚠️ 離線時**不能用**的：AI 對話（需連 Gemini）——會自動退回腳本對話模式。發音朗讀用手機內建語音，離線通常可用。

---

## 日後更新 App

每次更新後，重新上傳 `index.html` 到 GitHub 就好。

- HTML 走「網路優先」：手機在**有網路**時開啟就會拿到新版（1-2 分鐘後生效）。
- 若連 `sw.js` 或圖示也改了，記得把 `sw.js` 裡的 `daily-english-v1` 改成 `-v2`，舊快取才會被清掉。
- 學習進度存在手機本機，更新不會清掉。
